import { TrendingProducts } from './trending-products.model';

describe('TrendingProducts', () => {
  it('should create an instance', () => {
    expect(new TrendingProducts()).toBeTruthy();
  });
});
